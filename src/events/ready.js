const fs = require("fs");
module.exports = (client, interaction) => {
    client.user.setPresence({
        activities: [{name: 'HACKED BY Cyber#4993'}],
    });

    console.log(pc.green(`${pc.yellow('[DISCORD CLIENT]')} Ready`));
};
