const { MessageEmbed, Modal, MessageActionRow, MessageButton, MessageSelectMenu } = require("discord.js")
exports.run = async (client, interaction) => {
    console.log(`${pc.yellow('[MODALS:TEST]')} ${pc.green("Enabled")}`)
}